<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/title_activity_second_page"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.shortcuts"
            android:resource="@xml/shortcuts" />

        <activity
            android:name=".activityHomepage"
            android:exported="true"
            android:label="@string/activityHomepage"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activitySecondPage"
            android:exported="true"
            android:label="@string/title_activity_second_page"
            android:theme="@style/Theme.AppCompat"></activity>
        <activity
            android:name=".activityThirdScreen"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat"></activity>
        <activity
            android:name=".activityFourthScreen"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat"></activity>

        <activity
            android:name=".foodMenuRecycleView"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat"></activity>
    </application>

</manifest>